# Rust toolchain configuration for YachtSQL
#
# This file specifies the Rust toolchain version and components
# required to build and develop the project.

[toolchain]
channel = "nightly-2025-09-14"
components = ["rustfmt", "clippy"]
